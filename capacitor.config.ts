import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'advert.io.phaser',
  appName: 'advert.io.phaser',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
